namespace EleSy.CV.ActiveX.Model
{
    class ServerStatus
    {
        public enum ServerStatusENumSwitch
        {
            ServerIsConnected,
            ServerNotConnected,
            ConnectionFailed,
            ObjectPathNotExist,
            UnknownError
        }
    }
}
